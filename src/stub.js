/**
 * Creates an object that provides canned answers to calls made during the test, usually
 * not responding at all to anything outside what's programmed in
 * for the test.
 * @param {*} obj
 * @param {*} stubName
 */
module.exports = (obj, stubName) => {};
